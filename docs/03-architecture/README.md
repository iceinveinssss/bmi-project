# Этап 2 — Архитектурное проектирование (Недели 5–6)

Артефакты по методичке:

1) Диаграмма пакетов (структура слоёв PCMEF) — UML-диаграмма
2) Спецификация интерфейсов — контракты между слоями (документ/Java‑интерфейсы)
3) Диаграмма зависимостей — UML‑диаграмма
4) ADR (Architectural Decision Records) — архитектурные решения (документ)

## Что уже есть в коде

- Реальная структура пакетов PCMEF: `backend/src/main/java/com/bmi/*`
- Контракты слоёв: `com.bmi.mediator.*` (интерфейсы сервисов), `com.bmi.foundation.*` (репозитории)

## Чеклист заполнения

- [ ] Нарисовать диаграмму пакетов (P→C→M→E→F)
- [ ] Описать 3–5 ADR (например: JWT, PostgreSQL, Expo, JaCoCo)

